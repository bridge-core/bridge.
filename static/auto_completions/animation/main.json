{
    "format_version": "$general.format_version",
    "animations": {
        "$placeholder": {
            "animation_length": "$general.number",
            "loop": "$general.boolean",
            "timelines": {
                "$dynamic.timeline.next_step": {
                    "$dynamic.list.next_index": "$animation.general.main"
                }
            }
        }
    }
}