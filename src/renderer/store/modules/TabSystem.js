const state = {
    split_screen_active: false
}

const mutations = {
    setSplitScreenActive(state, val) {
        state.split_screen_active = val;
    }
}

const actions = {

}

export default {
    state,
    mutations,
    actions
}